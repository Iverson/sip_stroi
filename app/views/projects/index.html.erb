<h2 id="typical" class="h2 h2_center"><%= t("projects.index.typical") %></h2>
<div class="b-projects">
  <%= render(partial: "shared/project_thumb", collection: @typical_projects, as: :project) %>
</div>

<% unless @individual_projects.empty? %>
<br><br>
<h2 id="individual" class="h2 h2_center"><%= t("projects.index.individual") %></h2>
<div class="b-projects">
  <%= render(partial: "shared/project_thumb", collection: @individual_projects, as: :project) %>
</div>
<% end %>

<% unless @special_projects.empty? %>
<br><br>
<h2 id="special" class="h2 h2_center"><%= t("projects.index.special") %></h2>
<div class="b-projects">
  <%= render(partial: "shared/project_thumb", collection: @special_projects, as: :project) %>
</div>
<% end %>
