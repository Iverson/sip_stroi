<h1 class="h2 h2_center">Контакты</h1>

<div class="">
  <div class="l-center">
    <div class="b-volumes__text b-text">
      <div class="b-offices">
        <% @agencies.each do |office| %>
        <p><%= office.info.html_safe %></p>
        <% end %>
      </div>
    
      <br /><br />
      <%= c('about.contacts.text', :text) %>
    </div>
  </div>
</div>