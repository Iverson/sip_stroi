<!-- <div class="b-breadcrumbs">
  <a href="<%= articles_path %>">Статьи</a> / <span><%= @article.name %></span>
</div> -->

<div class="b-article">
  <p class="b-article__date"><%= I18n.l @article.created_at, :format => :long %></p>
  <h1 class="b-article-title"><%= @article.name %></h1>

  <%= image_tag @article.image.url(:medium), class: "b-article__pic" if @article.image? %>
  <div class="b-article__text"><%= @article.full_text.html_safe %></div>
</div>